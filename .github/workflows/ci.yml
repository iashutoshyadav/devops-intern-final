name: CI Pipeline

on: [push]

jobs:
  run-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Hello Script
        run: python hello.py
